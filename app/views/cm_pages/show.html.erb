<% content_for :breadcrumb do %>
  <li><%= link_to "Content Marketing Pages", cm_pages_path %> <span class="divider">/</span></li>
  <li class="active"><%= @cm_page.title %></li>
<% end %>
<p id="notice"><%= notice %></p>
<table class="table table-bordered table-hover" >
  <tr>
    <th>Page Title</th>
    <th>Page Content</th>
  </tr>
  <tr>
    <td><%= @cm_page.title %></td>
    <td><%= @cm_page.content %></td>
  </tr>
</table>

<%= link_to 'Edit Content Marketing Page', edit_cm_page_path(@cm_page) %> |
<%= link_to 'Back', :back %>

