<%= simple_form_for([@customer_account,@customer_site,@customer_contact]) do |c| %>
  <%= c.error_notification %>
    <%= c.input :first_name %>
    <%= c.input :last_name %>
    <%= c.input :phone_number1 %>
    <%= c.input :fax_number %>
    <%= c.input :email_addr %>
    <%= c.input :sell_to, :as => :radio_buttons %>
    <%= c.input :ship_to, :as => :radio_buttons %>
    <%= c.input :bill_to, :as => :radio_buttons %>
    <%= c.button :submit %>
  
<% end %>

